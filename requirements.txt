# Core vision / tracking (crashDetectionSystem, realTimeTracking)
opencv-python>=4.8,<5
ultralytics>=8.0,<9
numpy>=1.24,<2
pandas>=2.0,<3

# Deep learning - crash detector & DQN (vehicle-crash-detector, rlSUmo-main)
tensorflow>=2.10,<2.16
Pillow>=9.0,<11

# Firebase integration (all sub-projects)
firebase-admin>=6.0,<7

# Visualization / plotting (rlSUmo-main)
matplotlib>=3.5,<4

# Used by vehicle-crash-detector (IPython.display / HTML embedding)
ipython>=8.0,<9
